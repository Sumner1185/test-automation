const chai = require('chai');
const chaiHttp = require('chai-http');
const { expect } = chai;

chai.use(chaiHttp);

describe('Article API', () => {
 // TODO: Implement tests for Get, Delete and Post endpoint
});
